# storylab journal

Memory for the human-in-the-loop optimizer. Each `apply` appends the optimizer's
hypothesis; each `judge` appends a leaderboard snapshot. Included in every
`export` so the optimizer builds on what worked and avoids dead ends.
