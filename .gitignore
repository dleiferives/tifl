# Go build output
/bin/
*.test
*.out
coverage.txt

# Web (see web/.gitignore too)
/web/node_modules/
/web/dist/

# Local data & logs — never committed
/data/
/ai_logs/
*.db

# Local config — copy tifl.config.example.yaml to tifl.yaml and edit
/tifl.yaml

# Editors / OS
.DS_Store
*.swp
